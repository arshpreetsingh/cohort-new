# base Porjct Plan File


## Scope: Porjcet Scope is to have weekly classes for the students, where Student can regist and attend calsses.

## Requirements

# FrontEnd : Required pages: User Registeration, User Login, Available Classes, Dashboard(Providing information about Registered Users, Registered users for classes, etc), instructor information (static for now,) classes inforamtin(statuc for now)
## Try to use simple HTML, css and JAvascript for bacend API Calls

# backend

## Tbuild backend APIs for required UI pages (user registration, Users list, update user, Delete user, Admin account for user management, Admin Login(Keep admin login static and Set env variables))
## Backend an dfrontend will be using netlify, Create a netlify env-variable file to add required ENVs vairables, there, Bacend should be in JaveScript to make API calls to DB(Else try to suggest me better one here)

# Features
## user registration, user management, list of users, a simple user Registration system, user login and Authentication is not required, any ser with link can register, 

